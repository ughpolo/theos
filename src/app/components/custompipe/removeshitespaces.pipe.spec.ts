import { RemoveshitespacesPipe } from './removeshitespaces.pipe';

describe('RemoveshitespacesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveshitespacesPipe();
    expect(pipe).toBeTruthy();
  });
});
